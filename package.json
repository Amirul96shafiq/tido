{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "dev:full": "concurrently -n vite,php,queue -c cyan,green,yellow \"npm run dev\" \"php artisan serve --host=0.0.0.0 --port=2000\" \"php artisan queue:listen --queue=whatsapp,receipts,default --tries=3 --timeout=180\"",
        "dev:ollama": "node scripts/run-ollama.mjs",
        "evolution": "node scripts/run-evolution.mjs",
        "dev:whatsapp": "concurrently -n vite,php,queue,evolution -c cyan,green,yellow,magenta \"npm run dev\" \"php artisan serve --host=0.0.0.0 --port=2000\" \"php artisan queue:listen --queue=whatsapp,receipts,default --tries=3 --timeout=180\" \"npm run evolution\"",
        "dev:all": "concurrently -n vite,php,queue,evolution,ollama -c cyan,green,yellow,magenta,blue \"npm run dev\" \"php artisan serve --host=0.0.0.0 --port=2000\" \"php artisan queue:listen --queue=whatsapp,receipts,default --tries=3 --timeout=180\" \"npm run evolution\" \"npm run dev:ollama\""
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "prettier": "^3.9.6",
        "prettier-plugin-blade": "^3.2.2",
        "prettier-plugin-tailwindcss": "^0.8.1",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
